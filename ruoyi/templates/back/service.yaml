apiVersion: v1
kind: Service
metadata:
  name: {{ include "ruoyi.fullname" . }}
  labels:
    {{- include "ruoyi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ruoyi.selectorLabels" . | nindent 4 }}
